<h3 class="ui header">{{t 'forms.i-i-s-weather-forecast-possible-cataclysm-e.caption'}}</h3>

<form class="ui form flexberry-vertical-form" role="form">
  {{flexberry-error error=error}}
  <div class="field">
    <div class="sixteen wide">
      {{flexberry-validationsummary errors=model.errors}}
    </div>
  </div>
  <div class="field">
    <div class="flexberry-edit-panel">
      {{#unless readonly}}
        <button type="submit" class="ui button save-button" {{action 'save'}}>
          {{t "forms.edit-form.save-button-text"}}
        </button>
        {{#unless model.isNew}}
          <button type="submit" class="ui button save-del-button" {{action 'delete'}}>
            {{t "forms.edit-form.delete-button-text"}}
          </button>
        {{/unless}}
      {{/unless}}
      <button type="submit" class="ui button close-button" {{action 'close'}}>
        {{t "forms.edit-form.close-button-text"}}
      </button>
    </div>
  </div>
  <div class="field">
  <label>{{t 'forms.i-i-s-weather-forecast-possible-cataclysm-e.start-caption'}}</label>
  {{
    flexberry-datepicker
    placeholder=(t 'components.flexberry-datepicker.placeholder')
    readonly=readonly
    value=model.start
    class=(if model.errors.start 'error' '')
  }}
  {{flexberry-validationmessage error=model.errors.start pointing="pointing"}}
</div>

<div class="field">
  <label>{{t 'forms.i-i-s-weather-forecast-possible-cataclysm-e.finish-caption'}}</label>
  {{
    flexberry-datepicker
    placeholder=(t 'components.flexberry-datepicker.placeholder')
    readonly=readonly
    value=model.finish
    class=(if model.errors.finish 'error' '')
  }}
  {{flexberry-validationmessage error=model.errors.finish pointing="pointing"}}
</div>

</form>
